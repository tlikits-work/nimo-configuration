name: On Merge
run-name: "[On Merge] ${{ github.event.head_commit.message }}"

on:
  push:
    branches:
      - main

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Code Checkout
        uses: actions/checkout@v4

      - uses: ./.github/actions/use-node

      - name: Validate
        run: npm run validate

  tag:
    runs-on: ubuntu-latest
    permissions: write-all
    concurrency:
      group: version-tagging
    environment: tag
    needs: validate
    steps:
      - name: Code Checkout
        uses: actions/checkout@v4

      - name: Get new Tag
        id: get_new_tag
        uses: ./.github/actions/get-new-tag

      - name: Create tag
        uses: ./.github/actions/create-tag
        with:
          tag: "${{ steps.get_new_tag.outputs.tag_name }}"

      - name: Trigger Deployment
        uses: ./.github/actions/trigger-tag-event
        with:
          workflow_id: on-tag.yml
          tag: "${{ steps.get_new_tag.outputs.tag_name }}"
